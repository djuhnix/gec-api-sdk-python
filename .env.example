# Environment variables for GEC API Member Import
# ─────────────────────────────────────────────────────────────────────────────
# 1. Copy this file:  cp .env.example .env
# 2. Fill in your values below
# 3. Never commit .env to Git — it contains secrets
# ─────────────────────────────────────────────────────────────────────────────

# URL of the GEC API server — no trailing slash
# Local development:  http://localhost:8080
# Production:         https://api.gec-asso.fr
GEC_API_HOST=http://localhost:8080

# JWT bearer token used to authenticate with the API
# Ask your team admin if you don't have one.
# It looks like a long string starting with "eyJ..."
GEC_API_TOKEN=your_jwt_token_here

# ─────────────────────────────────────────────────────────────────────────────
# Google Sheets (optional — only needed for --source gsheet imports)
# ─────────────────────────────────────────────────────────────────────────────
# Path to your Google credentials file.
# - OAuth2 flow:       path to credentials.json downloaded from Google Cloud Console
# - Service account:   path to the service account JSON key file
# Leave blank if you are only importing from Excel files.
GOOGLE_CREDENTIALS_PATH=

